dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.36  # ✅ Вместо video_player для аудио
  # video_player: ^2.8.2  # ❌ Убираем

dev_dependencies:
  flutter_lints: ^3.0.1  # ✅ Для линтинга (опционально)
